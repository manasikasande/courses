/// import relevent files
import { Component } from "@angular/core";

// decorator
// contains meta data of the class
@Component({
    selector: 'app-server', // selector name should be unique
    templateUrl: './server.component.html'
})

// class will contain the blueprint of the component
export class ServerComponent {

}